# ドキュメント

この `docs/` ディレクトリは Docusaurus のドキュメントに対応するための初期ファイルを含んでいます。

推奨される次のステップ:

- 必要に応じて `docusaurus.config.js` と `website` または `docs` 用の設定を追加してください。
- `sidebars.js` を作成してドキュメントの目次を定義してください。
- GitHub Pages や Vercel にデプロイするための CI 設定を検討してください。
